dist: focal

jobs:
  include:
    - stage: test
      name: "Golang build and test"
      language: go
      env:
        - GO111MODULE=on
      go:
        - 1.13.x
      before_script:
        - go mod vendor
      script: 
        - make build
        - make test
    
    - stage: test
      name: "Node.js test"
      language: node_js
      node_js:
        - lts/*
      env:
        - NODE_ENV=test
      script:
        - make test-frontend
