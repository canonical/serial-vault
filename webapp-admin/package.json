{
  "name": "webapp-admin",
  "version": "0.4.1",
  "private": true,
  "dependencies": {
    "history": "^4.10.1",
    "jwt-decode": "^2.2.0",
    "moment": "^2.24.0",
    "npm": "^6.14.6",
    "react": "^16.12.0",
    "react-intl": "^3.11.0",
    "react-scripts": "^3.3.1",
    "then-request": "^6.0.2"
  },
  "scripts": {
    "build-css": "node-sass src/ -o src/",
    "start": "react-scripts start",
    "build": "npm run build-css && react-scripts build",
    "test": "npm run build-css && react-scripts test --env=jsdom",
    "test:ci": "npm run build-css && react-scripts test --env=jsdom --no-cache --reporters=jest-junit --bail --ci --coverage",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "enzyme": "^3.11.0",
    "enzyme-adapter-react-16": "^1.15.2",
    "jest-junit": "^10.0.0",
    "node-sass": "^4.14.0",
    "react-dom": "^16.12.0",
    "vanilla-framework": "^1.7.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
