title: "Serial Vault"
logo: "/static/images/logo-ubuntu-white.svg"

# Service mode: signing or admin
mode: signing

# Path to the assets (${docRoot}/static)
docRoot: "."

# Backend database details
driver: "postgres"
datasource: "host=localhost port=5432 dbname=identityvault user=rmescandon password=rmescandon"

# Signing Key Store
#keystore: "filesystem"
#keystorePath: "./keystore"

# For Database
keystore: "database"
keystoreSecret: "secret code to encrypt the auth-key hash"

# For TPM 2.0
#keystore: "tpm2.0"
#keystorePath: "./keystore"
#keystoreSecret: "this needs to be 32 bytes long!!"

# Valid API keys
apiKeys:
    - U2VyaWFsIFZhdWx0Cg
